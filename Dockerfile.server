FROM golang:1.26-alpine AS build
WORKDIR /app
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /connectivity-server ./cmd/server

FROM gcr.io/distroless/static-debian13
COPY --from=build /connectivity-server /connectivity-server
ENTRYPOINT ["/connectivity-server"]
